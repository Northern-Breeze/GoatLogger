// Root import is intentionally types-only — pick a platform entry for a runnable logger:
//   import { createLogger } from 'goatlogger/browser'
//   import { createLogger } from 'goatlogger/node'
export * from "./core/types";
